# Development Workflow

This repository uses a protected main branch. All changes must go through the dev branch and pull requests.

## Workflow:
1. Push changes to `dev` branch
2. Wait for CI/CD to pass
3. Create PR from `dev` to `main`
4. Get approval
5. Merge to `main`

